{% extends('base.html.twig') %}

{% block title %}
    Listado de usuarios | Symfony
{% endblock %}

{% block stylesheets %}
    <style>

    </style>
{% endblock %}

{% block body %}
    <div>
        <h1>Hola a todos</h1>
            <a href="{{path('createUser')}}" class="btn btn-default">Crear Usuario</a>
        <table>
            <thead>
                <tr>
                    <th>#</th>
                    <th>Nombre</th>
                    <th>Apellido</th>
                    <th>Correo</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            {#
            <tbody>
                {% for user in listUsers %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td>{{ user.name }}</td>
                        <td>{{ user.lastname }}</td>
                        <td>{{ user.email }}</td>
                        <td>{{ user.status }}</td>
                        {% if user.status === 1 %}
                            <td>
                                <a href="{{path('updateUser', {'id':user.idUser}) }}" class="btn btn-danger">Eliminar</a>
                                <a href="{{path('deleteUser', {'id':user.idUser}) }}" class="btn btn-primary">Editar</a>
                            </td>
                        {% endif %}
                    </tr>
                {% endfor %}
            </tbody>
            #}
        </table>
    </div>
{% endblock %}

{% block javascripts %}
    <script>

    </script>
{% endblock %}